Features

Open source

Secure cloud hosting

Allows to share secrets across different devices and keep
them in sync.

Allows to share secrets with other people and keep them in
sync (needs a common hosting platform).

Browser plugin.

Auto-fills web forms.

Peer to peer web forms database.

Reset password with one click.
